services:
  stt-api:
    build: .
    ports:
      - "80:8000"
    env_file: .env
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
