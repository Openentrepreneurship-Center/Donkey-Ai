services:
  stt-api:
    build: .
    ports:
      - "8000:8000"
    env_file: .env
    # GPU 사용 시 아래 주석 해제
    # deploy:
    #   resources:
    #     reservations:
    #       devices:
    #         - driver: nvidia
    #           count: 1
    #           capabilities: [gpu]
